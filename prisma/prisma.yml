endpoint: https://eu1.prisma.sh/nikolas-burk/hackernews-node-pc/dev
datamodel: datamodel.graphql

generate:
  - generator: javascript
    output: ../src/generated/prisma.js

hooks:
  post-deploy:
    - yarn graphql get-schema --project prisma
